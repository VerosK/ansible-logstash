---
logstash:
  package: 'debian/logstash_1.4.2-1-2c0f5a1_all.deb'
  binary: '/opt/logstash/bin/logstash'
  service_name: logstash
  dependencies:
    - 'openjdk-7-jre'

  # confirm OS is supported
  supported_os: True